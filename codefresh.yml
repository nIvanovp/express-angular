version: '1.0'

steps:
#  clone-project:
#    type: git-clone
#    repo: https://github.com/nCodefresh/express-angular.git

  build-project:
    type: build
    description: codefresh example
    image-name: gcr.io/cf-ci-1027/express-angular
    dockerfile: Dockerfile
    tag: ${{CF_BRANCH}}

  push-to-dockerhub:
    type: push
    candidate: ${{build-project}}
    tag: ${{CF_BRANCH}}
    registry: gcr.io
    credentials:
      username: ${{username}}
      password: ${{password}}
      email: ${{email}}
